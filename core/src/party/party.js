// party 彩蛋模块（core/src/party/party.js）
//
// ⚠️ 占位实现：原始的 runParty() 逻辑未随仓库提交（仅存在于作者的本地工作副本），
//    因此此处先用占位实现，保证 `install party` 命令可正常调用而不报错。
//    请作者把本地 party/party.js 中真正的 runParty() 实现粘贴替换本文件即可恢复彩蛋。

async function runParty() {
    console.log('');
    console.log('🎉 party 模式（占位实现）');
    console.log('   原始 party 彩蛋逻辑尚未恢复 —— 请把你本地 party/party.js 的 runParty 实现贴回此处。');
    console.log('');
}

module.exports = { runParty };
